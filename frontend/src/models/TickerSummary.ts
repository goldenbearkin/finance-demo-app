export interface TickerSummary {
  symbol: string;
  name: string;
  regularMarketPrice: string;
  regularMarketPriceChange: string;
  regularMarketPercentChange: string;
  regularMarketPriceAt: string;
}
